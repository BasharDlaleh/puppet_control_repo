# these modules will be imported automatically from forge.puppet.com (like ansible galaxy)
# you can write your own modules inside site folder 
# profile are a group of modules/resources that make up one thing like all the necessary resources (user, file, package, service) to create an NGINX web server
# roles are a group of profiles that define a role for the node machine, like a role that includes NGINX profile and MYSQL profile is a role for a dev server
# always use unique roles for nodes even if 2 nodes has the exact same role

mod 'puppet-nginx', '5.0.0'
mod 'puppetlabs-concat'
mod 'puppetlabs-stdlib',
